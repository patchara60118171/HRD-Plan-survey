{
  "name": "well-being-survey",
  "version": "1.0.0",
  "description": "Well-being Survey",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && cp -r css js admin assets surveys docs public/"
  },
  "dependencies": {}
}